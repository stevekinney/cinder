{
  "extends": "./tsconfig.json",
  "//": "src/lib/editor/package-subpath.test.ts self-imports '@lostgradient/editor/editor/*' by bare specifier to prove the export map resolves at RUNTIME (bun test). tsc resolves that subpath's 'types' condition to dist/, which does not exist yet when this package's own `typecheck` task runs before its own `build` task in turbo's schedule (unlike a downstream package's cross-package imports, which resolve fine because upstream packages build first). Excluded here — same pattern as components/tsconfig.check.json excluding src/index.ts and src/components.test.ts for the identical reason — and still covered at runtime by `bun test`.",
  "exclude": [
    "node_modules",
    "coverage",
    "dist",
    "**/.cinder-ssr-*",
    "**/.editor-ssr-*",
    "src/lib/editor/package-subpath.test.ts"
  ]
}
