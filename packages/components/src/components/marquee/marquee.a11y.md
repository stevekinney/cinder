`Marquee` automatically duplicates its content for seamless looping and hides the duplicate from assistive technology.

Provide `label` when the marquee region needs an accessible name, and ensure interactive children remain keyboard reachable because `pauseOnFocus` defaults to `true`.
