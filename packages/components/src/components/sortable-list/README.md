# SortableList

A SortableList component. Replace this sentence with a one-line purpose statement once the migration settles.

## Usage

```svelte
<script lang="ts">
  import SortableList from 'cinder/sortable-list';
</script>

<SortableList />
```

## Props

<!-- generated:props:start -->

| Prop                | Type       | Required | Default | Description                                                            |
| ------------------- | ---------- | -------- | ------- | ---------------------------------------------------------------------- |
| `class`             | `string`   | no       | —       |                                                                        |
| `label`             | `string`   | no       | —       | Accessible name for the list (applied as aria-label on the list root). |
| `announcements`     | `(opaque)` | —        | —       | unknown-shape                                                          |
| `children`          | `(opaque)` | —        | —       | function-or-snippet                                                    |
| `formatHandleLabel` | `(opaque)` | —        | —       | function-or-snippet                                                    |
| `getItemLabel`      | `(opaque)` | —        | —       | function-or-snippet                                                    |
| `getKey`            | `(opaque)` | —        | —       | function-or-snippet                                                    |
| `handle`            | `(opaque)` | —        | —       | function-or-snippet                                                    |
| `items`             | `(opaque)` | —        | —       | generic-type-parameter                                                 |
| `onreorder`         | `(opaque)` | —        | —       | function-or-snippet                                                    |

<!-- generated:props:end -->

## CSS Variables

<!-- generated:variables:start -->

This component does not declare any local CSS variables.

<!-- generated:variables:end -->

## Subcomponents

<!-- generated:subcomponents:start -->

None.

<!-- generated:subcomponents:end -->
