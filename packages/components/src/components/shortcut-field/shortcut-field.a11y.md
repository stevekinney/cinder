# ShortcutField · accessibility

ShortcutField uses a read-only textbox role, captures only while focused, announces captures and validation messages in a polite live region, and provides a labelled clear button.
