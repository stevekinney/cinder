# PolicyLock · accessibility

PolicyLock's nearest neighbors are Badge and Tooltip. It is admitted because it combines the visual lock affordance with a durable, described policy explanation and scope taxonomy. The reason and source remain available to assistive technology; scope is visible text, so the state never relies on iconography or color alone. It adds no keyboard interaction.
