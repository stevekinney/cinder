{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "disabled": {
      "type": "boolean",
      "description": "Override the group's `disabled` for this single radio."
    },
    "id": {
      "type": "string",
      "description": "Unique identifier — required for label association."
    },
    "label": {
      "type": "string",
      "description": "Visible label rendered in a `<label>` element associated via `for`."
    },
    "description": {
      "type": "string",
      "description": "Helper text rendered as `<p id=\"{id}-description\">`, wired via aria-describedby."
    }
  },
  "additionalProperties": false,
  "required": ["id", "label"],
  "metadata": {
    "unsupportedProps": [
      {
        "name": "class",
        "reason": "unknown-shape"
      },
      {
        "name": "value",
        "reason": "unknown-shape"
      }
    ]
  }
}
