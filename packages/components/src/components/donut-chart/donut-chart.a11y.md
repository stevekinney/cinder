# DonutChart design and accessibility review

The chart exposes a figure label, keyboard-focusable series buttons when a callback is supplied, exact values in a text legend, and a center total. The optional scrollable wrapper handles narrow layouts. A true pie variant is intentionally not provided.
