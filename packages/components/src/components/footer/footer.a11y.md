# Footer accessibility notes

- Uses native `<footer>` semantics.
- Accepts an optional accessible name via `aria-label`.
- Link groups are rendered with headings and list semantics for clear structure.
- Legal links remain keyboard-accessible anchors in source order.
