# GuidanceRegion accessibility review

Guidance is opt-in and does not hijack focus. Consumers choose anchored `Popover` or modal `useModal` presentation; the registry owns claim eligibility, not presentation or storage outside the supplied adapter.
