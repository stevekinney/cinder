{
  "$schema": "../../schemas/examples.schema.json",
  "component": "code-block",
  "import": "@lostgradient/cinder/code-block",
  "examples": [
    {
      "id": "basic",
      "title": "Basic code block",
      "description": "Multi-line preformatted code.",
      "code": "<script lang=\"ts\">\n  import { CodeBlock } from '@lostgradient/cinder/code-block';\n  const code = `function greet(name: string): string {\n  return \\`Hello, \\${name}!\\`;\n}\n\nconst message = greet('world');`;\n</script>\n\n<CodeBlock {code} />\n"
    },
    {
      "id": "with-language",
      "title": "With language label and copy",
      "description": "Header shows the language and a copy-to-clipboard button. Setting a language is all it takes — CodeBlock lazy-loads the bundled Shiki highlighter on the client and highlights automatically, no extra wiring.",
      "code": "<script lang=\"ts\">\n  import { CodeBlock } from '@lostgradient/cinder';\n\n  const code = `SELECT id, name, created_at\nFROM users\nWHERE active = true\nORDER BY created_at DESC\nLIMIT 10;`;\n</script>\n\n<CodeBlock {code} language=\"sql\" copyable />\n"
    }
  ]
}
