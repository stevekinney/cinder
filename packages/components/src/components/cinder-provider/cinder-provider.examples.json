{
  "$schema": "../../schemas/examples.schema.json",
  "component": "cinder-provider",
  "import": "cinder/cinder-provider",
  "examples": [
    {
      "id": "basic",
      "title": "Mount one provider at the root",
      "description": "Wrap your app root with <CinderProvider highlighter={shikiHighlighter()}> once. The bundled cinder/highlighters/shiki adapter is the recommended default — every descendant <CodeBlock> picks it up via context.",
      "code": "<script lang=\"ts\">\n  import { CinderProvider, CodeBlock } from 'cinder';\n  import { shikiHighlighter } from 'cinder/highlighters/shiki';\n\n  const highlighter = shikiHighlighter();\n\n  const tsCode = `function greet(name: string): string {\n  return \\`Hello, \\${name}!\\`;\n}`;\n\n  const sqlCode = `SELECT count(*) FROM users WHERE active = true;`;\n</script>\n\n<CinderProvider {highlighter}>\n  <CodeBlock code={tsCode} language=\"ts\" copyable />\n  <CodeBlock code={sqlCode} language=\"sql\" />\n</CinderProvider>\n"
    },
    {
      "id": "nested-override",
      "title": "Nested provider overrides the root",
      "description": "Mount a second <CinderProvider> deeper in the tree to use a different highlighter for a subtree. The nearest provider wins. Useful for previewing a different theme or grammar set without touching the rest of the app.",
      "code": "<script lang=\"ts\">\n  import { CinderProvider, CodeBlock } from 'cinder';\n  import { shikiHighlighter } from 'cinder/highlighters/shiki';\n\n  const lightHighlighter = shikiHighlighter({ theme: 'github-light' });\n  const darkHighlighter = shikiHighlighter({ theme: 'github-dark' });\n\n  const code = `function greet(name: string): string {\n  return \\`Hello, \\${name}!\\`;\n}`;\n</script>\n\n<CinderProvider highlighter={lightHighlighter}>\n  <p class=\"example-preview-row\" style:display=\"block\">\n    <strong>Outer provider (github-light):</strong>\n  </p>\n  <CodeBlock {code} language=\"ts\" />\n\n  <CinderProvider highlighter={darkHighlighter}>\n    <p class=\"example-preview-row\" style:display=\"block\">\n      <strong>Inner provider (github-dark) — nearest wins:</strong>\n    </p>\n    <CodeBlock {code} language=\"ts\" />\n  </CinderProvider>\n</CinderProvider>\n"
    }
  ]
}
