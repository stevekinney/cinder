# KeyValueEditor design and accessibility review

The component composes labeled Input controls and SecretValueField for secret keys. Rows use list semantics, native tab order, and explicit remove labels. Secret values remain masked and are never placed in passive attributes.
