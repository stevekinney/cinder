# ParameterField · accessibility

The visible label names the editor region. The override rail is supplemental: the component also exposes override state through the Reset to default action, so color is never the only signal. The reset control is keyboard accessible and its tooltip includes the inherited value and unit. Unsaved and Experimental badges use visible text rather than color alone.
