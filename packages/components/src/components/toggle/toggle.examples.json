{
  "$schema": "../../schemas/examples.schema.json",
  "component": "toggle",
  "import": "@lostgradient/cinder/toggle",
  "examples": [
    {
      "id": "basic",
      "title": "Basic toggle",
      "description": "Switch using the WAI-ARIA switch pattern (aria-checked reflects state).",
      "code": "<script lang=\"ts\">\n  import { Toggle } from '@lostgradient/cinder/toggle';\n\n  let emailNotifications = $state(false);\n</script>\n\n<Toggle id=\"email-notifications\" bind:checked={emailNotifications} label=\"Email notifications\" />\n<p style=\"margin-top: 0.5rem; color: var(--cinder-text-muted);\">\n  {emailNotifications ? 'Notifications enabled' : 'Notifications disabled'}\n</p>\n"
    },
    {
      "id": "group",
      "title": "Multiple switches",
      "description": "Several independent on/off settings inside a fieldset. Each Toggle renders its own visible label, so no external <label for> wrapper is needed.",
      "code": "<script lang=\"ts\">\n  import { FormSection } from '@lostgradient/cinder/form-section';\n  import { Toggle } from '@lostgradient/cinder/toggle';\n\n  let emailNotifications = $state(true);\n  let darkMode = $state(false);\n  let autoSave = $state(true);\n</script>\n\n<FormSection as=\"fieldset\" heading=\"Account preferences\" columns={1}>\n  <Toggle id=\"pref-email\" bind:checked={emailNotifications} label=\"Email notifications\" />\n  <Toggle id=\"pref-dark\" bind:checked={darkMode} label=\"Dark mode\" />\n  <Toggle id=\"pref-autosave\" bind:checked={autoSave} label=\"Auto-save drafts\" />\n</FormSection>\n"
    },
    {
      "id": "hide-label",
      "title": "Icon-only / inline row",
      "description": "In a toolbar or table row where the meaning is already clear from context, hideLabel removes the visible text while keeping it as the accessible name.",
      "code": "<script lang=\"ts\">\n  import { Toggle } from '@lostgradient/cinder/toggle';\n\n  let wordWrap = $state(true);\n  let minimap = $state(false);\n</script>\n\n<div\n  role=\"toolbar\"\n  aria-label=\"Editor options\"\n  style=\"display: flex; align-items: center; gap: 1rem;\"\n>\n  <Toggle id=\"opt-wrap\" bind:checked={wordWrap} label=\"Word wrap\" hideLabel />\n  <Toggle id=\"opt-minimap\" bind:checked={minimap} label=\"Minimap\" hideLabel />\n</div>\n"
    }
  ]
}
