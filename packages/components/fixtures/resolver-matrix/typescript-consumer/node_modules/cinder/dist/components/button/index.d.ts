/**
 * Resolver-matrix fixture stub for `cinder/button` types. Only the type shape
 * needs to be importable; the runtime is irrelevant for `tsc --noEmit`.
 */
export type ButtonProps = {
  label: string;
  disabled?: boolean;
};

declare const Button: (props: ButtonProps) => unknown;
export default Button;
