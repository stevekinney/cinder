---
'@lostgradient/cinder': patch
---

Add descriptions for previously-undocumented public props so every prop now renders a Description in its generated README table and JSON schema (#373). Documentation only — no runtime or type-shape changes.
