---
'@lostgradient/cinder': patch
---

Use an interpreted JSON Schema runtime for the JSON Schema Editor and SchemaForm so editable schemas validate under Content Security Policies that block dynamic code generation.
