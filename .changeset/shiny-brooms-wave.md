---
'@lostgradient/cinder': minor
---

Add a new `Meter` component for bounded measurements with `role="meter"` semantics, threshold-aware state mapping, generated docs/schema artifacts, and playground examples.
