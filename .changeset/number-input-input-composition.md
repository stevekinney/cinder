---
'@lostgradient/cinder': patch
---

Compose NumberInput's editable control through Input while preserving locale-aware parsing, validation, native attachments, stepper behavior, and the visible-frame class hook. Input now exposes `groupClassName` for styling composed grouped controls.
