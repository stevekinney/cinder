---
'@lostgradient/cinder': minor
---

Add a new `InlineLoading` component for inline async-action lifecycle feedback (`inactive | active | finished | error`), with `description`, `iconDescription`, and `successDelay` props, polite live-region announcements, and auto-reset from `finished` back to `inactive`.
