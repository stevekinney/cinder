---
'@lostgradient/chat': patch
---

Clarify that defined `typingParticipants` and `readReceipts` props are authoritative, including empty arrays and maps, and that only `undefined` allows adapter pushes to drive those states.
