---
'@lostgradient/cinder': patch
---

Close the runtime coverage ratchet's remaining gap and wire it into CI (unit-tests.yaml and main-green.yaml now run `test:coverage` for this package alongside Chat's). No public API changes: two internal functions (`composedFocusScopes`, the shadow-slot walk in `collectComposedElements`) were restructured in behavior-preserving ways to eliminate lines Bun's coverage instrumentation could never attribute a hit to even when the branch executes, and the internal `chart-utilities.ts` barrel now also re-exports `createHorizontalCategoryLabelLayout` alongside the chart helpers it already exposed. `waitForTransitionCompletion`'s SSR/no-`requestAnimationFrame` fallback is unchanged from its original shape — it turned out to be a genuinely untested branch rather than a coverage artifact, so it's covered by a real test instead. `evaluateContainerSizeConstraints`'s compound width+inline-size recursive branch is marked as unreachable (with a from-source proof, not a restructure) since every validated call path already splits that condition before this function ever sees it.
