---
'@lostgradient/cinder': patch
---

Close the runtime coverage ratchet's remaining gap and wire it into CI (unit-tests.yaml and main-green.yaml now run `test:coverage` for this package alongside Chat's). No public API changes: two internal functions (`composedFocusScopes` and the shadow-slot walk in `collectComposedElements`) were restructured in behavior-preserving ways to eliminate lines Bun's coverage instrumentation could never attribute a hit to even when the branch executes, and `collectComposedElements` also gained a real test for the fallback-content case it was already handling correctly but nothing exercised. The internal `chart-utilities.ts` barrel now also re-exports `createHorizontalCategoryLabelLayout` alongside the chart helpers it already exposed. `waitForTransitionCompletion`'s SSR/no-`requestAnimationFrame` fallback is unchanged from its original shape — it turned out to be a genuinely untested branch rather than a coverage artifact, so it's covered by a real test instead. Two branches are provably unreachable rather than merely hard to hit, each closed by the method that fits it: `evaluateContainerSizeConstraints`'s compound width+inline-size recursive branch is marked in place with a `cinder-coverage-unreachable` source comment (every validated call path already splits that condition before this function ever sees it), while `splitScopeOutsideContext`'s dead ternary arm is collapsed onto its own guard clause instead, since `normalizeScopeRelativeSelector`'s mandatory preprocessing already rules that shape out before this function runs.
