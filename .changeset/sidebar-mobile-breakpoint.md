---
'@lostgradient/cinder': patch
---

Add a `mobileBreakpoint` prop to Sidebar so app shells can raise or lower the drawer breakpoint while keeping the JavaScript media query and Sidebar presentation contract aligned.
